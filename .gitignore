# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# uv / virtualenvs
.venv/
venv/
# uv.lock is committed — this is an application, not a library; CI needs a
# stable lockfile to build reproducibly.

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
.coverage.*

# Editor
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Claude Code session data (never commit)
.claude/

# cordon checks engine: generated report + CI checkout of the cordon repo
.cordon-checks-report.md
/.cordon/
