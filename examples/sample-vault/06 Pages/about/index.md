---
title: About
description: >-
  Example "about" page that ships from the vault to the public site.
intro: An example of a site page whose source is a vault note.
path: /about/
published: true
---

This is an example page. In a real vault, `06 Pages/<page>/index.md` is the
source for a route on the public site (`/about/` here) — the same markdown
Obsidian renders, published unchanged to the edge.

Pages use a lighter, site-facing frontmatter (`title`, `description`, `intro`,
`path`, `published`) rather than the operational doc schema, because they are
content for the website, not runbooks.
