[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "severino-vault-mcp"
version = "1.0.0"
description = "Local stdio MCP server that grounds AI assistants in Obsidian-style operational vaults with resources, runbooks, and safety gates."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Joseph Severino", email = "github@jseverino.com" }]
keywords = ["mcp", "model-context-protocol", "obsidian", "runbooks", "local-first", "ai-tools"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries",
]
dependencies = [
    "mcp>=1.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[project.scripts]
severino-vault-mcp = "severino_knowledge_router.__main__:main"
severino-knowledge-router = "severino_knowledge_router.__main__:main"

[project.urls]
Homepage = "https://github.com/joeseverino/severino-vault-mcp"
Issues = "https://github.com/joeseverino/severino-vault-mcp/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/severino_knowledge_router"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM"]
ignore = ["E501"]  # line length is enforced softly

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers"
pythonpath = ["src"]
